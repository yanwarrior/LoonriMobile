import { CartAddSerializer } from './cart-add-serializer';

describe('CartAddSerializer', () => {
  it('should create an instance', () => {
    expect(new CartAddSerializer()).toBeTruthy();
  });
});
