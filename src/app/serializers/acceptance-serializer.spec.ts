import { AcceptanceSerializer } from './acceptance-serializer';

describe('AcceptanceSerializer', () => {
  it('should create an instance', () => {
    expect(new AcceptanceSerializer()).toBeTruthy();
  });
});
