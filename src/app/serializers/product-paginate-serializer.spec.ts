import { ProductPaginateSerializer } from './product-paginate-serializer';

describe('ProductPaginateSerializer', () => {
  it('should create an instance', () => {
    expect(new ProductPaginateSerializer()).toBeTruthy();
  });
});
