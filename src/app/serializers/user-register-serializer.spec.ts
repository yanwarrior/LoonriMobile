import { UserRegisterSerializer } from './user-register-serializer';

describe('UserRegisterSerializer', () => {
  it('should create an instance', () => {
    expect(new UserRegisterSerializer()).toBeTruthy();
  });
});
