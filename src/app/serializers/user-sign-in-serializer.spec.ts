import { UserSignInSerializer } from './user-sign-in-serializer';

describe('UserSignInSerializer', () => {
  it('should create an instance', () => {
    expect(new UserSignInSerializer()).toBeTruthy();
  });
});
